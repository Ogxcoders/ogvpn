<?xml version="1.0" encoding="utf-8"?>
<!-- Release defaults: cleartext HTTP is disabled; the app talks to the
     control plane over TLS only. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
