<?xml version="1.0" encoding="utf-8"?>
<!-- Debug overlay: allow cleartext to the emulator loopback address so the
     debug build can reach a backend running on the host machine
     (10.0.2.2 is the host's address from inside the Android emulator).
     This file replaces the release network_security_config in debug builds. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
